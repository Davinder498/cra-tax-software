{
  "name": "cra-tax-starter",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev:backend": "pnpm --filter @app/backend dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "qa": "pnpm --filter @app/qa-harness start"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  },
  "packageManager": "pnpm@9.11.0"
}